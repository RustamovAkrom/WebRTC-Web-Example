# app/ — production qatlam: config, DB, modellar, auth, routerlar.
#
# Mavjud flat `main.py` va `rooms.py` (signaling yadrosi) o'z joyida qoladi.
# Bu paket faqat USTIGA qo'shiladi — signaling oqimini buzmaydi.
